{
  "name": "journeyhub-worker",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:schema:remote": "wrangler d1 execute journeyhub --remote --file=./schema.sql",
    "db:schema:local": "wrangler d1 execute journeyhub --local --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
